from django.apps import AppConfig

class CustomBlocksConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "customblocks"
    verbose_name = "Custom StreamField Blocks Library"

